// Q.2: Create an empty list of type string called days. Use the add method to add names of 7 days and print all days.

void main() {
  List<String> daYs = [];
  daYs.add("Monday");
  daYs.addAll(
      ["Tuesday", "Wednesday", "Thurday", "Friday", "Saturday", "Sunday"]);
  print(daYs);
}
